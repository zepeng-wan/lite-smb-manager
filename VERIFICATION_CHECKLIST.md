# Windows 与真实 SMB 人工验收清单

在 Windows 10/11 x64 的普通用户会话中执行，且使用拥有合法权限的测试共享。请勿以管理员身份启动。

- [ ] 解压 `dist\LiteSMBManager`，双击 `LiteSMBManager.exe`；主窗口和系统托盘图标出现，且无 Qt/DLL 错误。
- [ ] 新建配置：填写真实 `\\服务器\共享`、空闲盘符、账号密码，勾选“保存密码”和“自动连接”；非法 UNC、
  `C:` 或已在应用配置中重复的盘符会被阻止并显示中文提示。
- [ ] 退出并重新打开程序：配置仍存在，密码编辑框为空且未显示真实密码。
- [ ] 点击“连接”：状态变为“已连接”；资源管理器中看到目标盘，能在权限允许时读取测试文件。
- [ ] 单击任意配置行（尤其是已有 `nas` 配置）：整行选中后，“编辑、复制、删除、连接、测试连接”立即可用；
  在未连接时“断开、重新连接、打开资源管理器”保持禁用。用键盘上下箭头重复确认一次。
- [ ] 点击“连接”后，在 1 秒内确认该行变为“已连接”，选择没有丢失，且“断开、重新连接、打开资源管理器”
  变为可用、“连接”变为禁用；“所选配置”菜单中的同名项与底部按钮状态一致。
- [ ] 点击“刷新”：确认状态仍与资源管理器中的真实盘符一致。若将该盘符手工改映射到其他 UNC，应用应显示
  “存在冲突”且拒绝直接断开。
- [ ] 点击“打开资源管理器”：打开目标盘；点击“断开”：仅目标盘消失，其他网络盘不受影响。
- [ ] 为同一服务器用不同账号制造 Windows 1219：提示解释冲突，不自动断开任何其他连接。
- [ ] 新建多个配置并使用“连接全部/断开全部”：确认单项失败不会阻止其他项，且状态栏给出成功/失败/跳过摘要。
- [ ] 对一个失败的连接操作，确认状态显示“连接失败”，随后编辑、复制、删除、重新连接和测试连接仍可使用；
  不得出现按钮永久灰显。
- [ ] 修改/删除配置：删除时先确认；选择删除凭据后在 Windows Credential Manager 确认仅
  `LiteSMBManager/<该配置 id>` 被删除。
- [ ] 从设置启用登录启动并重新登录：程序自动启动，无管理员提示；随后可关闭该选项。
- [ ] 关闭窗口：默认隐藏至托盘，托盘菜单可显示、连接全部、断开全部和退出；设置改为完全退出后验证退出。
- [ ] 导出配置：打开 JSON，确认没有密码、`credential_ref` 或 `LiteSMBManager/`；导入同一文件时冲突条目有摘要，
  不会无提示覆盖。
- [ ] 在 `%APPDATA%\LiteSMBManager`、`%LOCALAPPDATA%\LiteSMBManager\logs` 和导出文件中搜索测试密码，确认不存在。

如果需要自动化真实挂载，请先确认测试盘符未使用，再设置 `SMB_TEST_UNC`、`SMB_TEST_USER`、
`SMB_TEST_PASSWORD`、`SMB_TEST_DRIVE`（可选 `SMB_TEST_DOMAIN`），并运行
`.\scripts\test.ps1 -Integration`。测试结束后复核盘符已精确清理。
